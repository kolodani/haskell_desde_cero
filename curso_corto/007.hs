module Test where

-- usamos la notacion v :: T
-- se puede leer v tiene el tipo T

-- calcula el doble
doble :: Int -> Int
doble x = 2 * x

-- una expresion debe tener un tipo que es calculado antes de su ejecucion